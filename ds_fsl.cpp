//
//
//线性表的合并
// 
//init list  线性表的线性表示 
bool InintList(&L)
{
	L.elem = (Elem*) malloc(sizeof(SIZE*sizeof(Elem));
	if(!L.elem) exit(OVERFLOW) //alloc failure
	L.ength = 0;
	L.listsize = SIZE;
	return OK;	
} 
//线性表的链式表示 
//
// 
//栈和队列操作的 线性表的子集
//
				
